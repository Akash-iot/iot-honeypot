# iot-honeypot
ESP32-based honeypot that simulates an IoT web UI and common services (HTTP, FTP, SSH, MQTT). Detects SQL injection and credential attacks, issues admin OTPs, sends alerts to Blynk, and persists offender IP blocks to SPIFFS. Built for research and defensive testing — do not deploy without authorization.
